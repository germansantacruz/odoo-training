<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_library_book_format_list" model="ir.ui.view">
        <field name="name">library.book.format.list</field>
        <field name="model">library.book.format</field>
        <field name="arch" type="xml">
            <list string="Formatos de Libro" delete="0" editable="bottom">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="action_library_book_format" model="ir.actions.act_window">
        <field name="name">Formatos de Libro</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">library.book.format</field>
        <field name="view_mode">list</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crea tu primer formato de libro
            </p>
        </field>
    </record>
</odoo>